[package]
name = "tnf_common"
version = "0.1.0"
authors = ["qthree <qthree3@gmail.com>"]
edition = "2018"
publish = false

[features]
default = []
client = ["winapi", "dll"]
server = ["winapi", "dll"]
dll = []
bridge = ["bincode", "parking_lot", "once_cell"]

[dependencies]
winapi = { optional=true, version = "0.3.6", features = ["consoleapi"] }
serde = { version = "1.0.93", features = ["derive"] }

bincode = { version = "1.1.4", optional = true }
parking_lot = { version = "0.8.0", optional = true }
once_cell = { version = "0.2.1", optional = true }


#[lib]
#crate-type = ["cdylib"]
